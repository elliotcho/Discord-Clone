query DirectMessages($receiverId: Int!) {
    directMessages(receiverId: $receiverId) {
        id
        text
        createdAt
        pic

        user {
            ...RegularUser
        }
    }
}